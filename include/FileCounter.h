#ifndef FILECOUNTER_H
#define FILECOUNTER_H

#include <string>
#include <vector>
#include <algorithm>
#include <iterator>
#include <windows.h>

class FileCounter
{
    public:
        FileCounter();
        ~FileCounter();

    private:
};

#endif // FILECOUNTER_H
